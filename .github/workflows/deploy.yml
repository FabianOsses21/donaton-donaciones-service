name: CI/CD Donaciones Service

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:
  build-test-docker:
    runs-on: ubuntu-latest

    steps:
      - name: Descargar codigo
        uses: actions/checkout@v4

      - name: Configurar Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: maven

      - name: Ejecutar pruebas unitarias y generar JAR
        working-directory: donaciones-service
        run: mvn clean package

      - name: Construir imagen Docker
        working-directory: donaciones-service
        run: docker build -t donaciones-service:latest .